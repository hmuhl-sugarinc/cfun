#include <stdio.h>

int main() {
  // Declare an array of ints and initialize it with unique values

  // Declare an integer pointer, and initialize it with the address of your array

  // Print the value stored in the pointer

  // Dereference the pointer and print the value the pointer points to

  // Add 1 to the pointer

  // Print the value stored in the pointer

  // Dereference the pointer and print the value the pointer points to

}